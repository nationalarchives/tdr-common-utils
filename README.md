# TDR Common Utils

Common libraries and code used across TDR

## Create Snapshot Locally

To create a snapshot version locally to use with local development run the following command:

```sbt publishLocal```

The snapshot version can then be imported into local development environments.
